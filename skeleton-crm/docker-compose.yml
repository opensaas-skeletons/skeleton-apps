services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: skeleton_crm
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "0.0.0.0:5436:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "0.0.0.0:3005:3005"
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: skeleton_crm
      DB_USER: postgres
      DB_PASSWORD: postgres
      PORT: 3005
      CORS_ORIGIN: "*"
      SEED_ON_START: "true"
    depends_on:
      db:
        condition: service_healthy

  client:
    build:
      context: .
      dockerfile: client/Dockerfile
    ports:
      - "0.0.0.0:5177:5177"
    environment:
      API_URL: http://server:3005
    depends_on:
      - server

volumes:
  pgdata:
